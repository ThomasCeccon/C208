#1) código fonte(Linguagem assembly)->montador(tradutor assembler)->código objeto(linguagem de maquina)->Link-edicao(ligador que gera o código executável a partir de bibliotecas)
#2)1-verdade 2-falso na arquitetura CISC o tempo de execuçao que é maior, pois precisa gerar um microcódigo
#3) move $t1,$t0,$0 passando para instrução basica add $t1,$t0,$0

#4)
#
#
#
#